# IMDbProject
Program to get data from a text file of movie names using a IMDb API, storing the data in a CSV file, and generating a movie recommendation based on specified genre, actor, director etc.
